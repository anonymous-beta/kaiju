# ── KAIJU core ──────────────────────────────────────────
mcp>=1.2.0                # Model Context Protocol SDK
rich>=13.7.0              # beautiful terminal output
questionary>=2.0.0        # interactive AI API setup wizard
requests>=2.31.0          # HTTP everywhere
pyfiglet>=1.0.3           # banner typography
colorama>=0.4.6           # cross-platform ANSI

# ── recon / network ────────────────────────────────────
dnspython>=2.4.0          # DNS enumeration
python-whois>=0.8.0       # domain whois
beautifulsoup4>=4.12.0    # HTML parsing

# ── optional (uncomment only if you extend KAIJU) ───────
# scapy>=2.5.0            # packet crafting (not imported yet)
# python-nmap>=0.7.1      # nmap wrapper (code shells out to system nmap)
